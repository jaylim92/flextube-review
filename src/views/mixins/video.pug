mixin video(video)
    div 
        h1
            a(href=`/videos/${video.id}`) #{video.title}
        p=video.description
        ul 
            each hashtag in video.hashtags 
                li=hashtag
        small=video.createAt
        hr